<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="3.9.5">Jekyll</generator><link href="https://felipe-parodi.github.io//feed.xml" rel="self" type="application/atom+xml" /><link href="https://felipe-parodi.github.io//" rel="alternate" type="text/html" /><updated>2026-03-06T11:10:35-08:00</updated><id>https://felipe-parodi.github.io//feed.xml</id><title type="html">Felipe Parodi</title><subtitle>Computational Neuroethologist | PhD Candidate at UPenn | Research in AI, Deep Learning, and Primate Behavior | Specializing in Social Interaction Analysis</subtitle><author><name>Felipe Parodi</name></author><entry><title type="html">How Register Tokens Reshape Information Flow in Vision Transformers</title><link href="https://felipe-parodi.github.io//blog/register-tokens-information-flow/" rel="alternate" type="text/html" title="How Register Tokens Reshape Information Flow in Vision Transformers" /><published>2026-03-04T00:00:00-08:00</published><updated>2026-03-04T00:00:00-08:00</updated><id>https://felipe-parodi.github.io//blog/register-tokens-information-flow</id><content type="html" xml:base="https://felipe-parodi.github.io//blog/register-tokens-information-flow/"><![CDATA[<div class="howtocv-links">
  <a href="https://github.com/felipe-parodi/howtocv" class="howtocv-link-btn"><i class="fab fa-github"></i> Code</a>
  <a href="#" class="howtocv-link-btn"><i class="fas fa-file-pdf"></i> Paper PDF</a>
  <a href="#citation" class="howtocv-link-btn"><i class="fas fa-quote-left"></i> Cite</a>
</div>

<p>When neural networks process images, they need to organize a lot of information — and some of the most capable models have quietly developed extra “scratch space” to do it. Modern vision transformers like DINOv2 and DINOv3 include an unusual architectural feature: <strong>register tokens</strong> — extra learned tokens added alongside the image tokens that participate in the network’s computations but don’t correspond to any image region. Originally introduced to <a href="https://arxiv.org/abs/2309.16588">suppress attention artifacts</a>, these tokens turn out to play a far more active role than their name suggests.</p>

<p>In this post, I walk through our findings from systematic token-zeroing experiments — selectively disabling parts of the model and measuring what breaks. We find an <strong>asymmetric dissociation</strong> between CLS and register tokens — CLS zeroing selectively spares dense tasks when registers are present, but register zeroing degrades <em>everything</em>. We show that registers function as <strong>attention scaffolds</strong> rather than information stores, and trace the <strong>temporal dynamics</strong> of how registers acquire and sometimes release semantic content across network layers. The upshot: register tokens are load-bearing computational infrastructure, not passive bystanders.</p>

<hr />

<h2 id="the-dino-family-and-the-register-problem">The DINO Family and the Register Problem</h2>

<h3 id="self-supervised-vits-in-60-seconds">Self-supervised ViTs in 60 seconds</h3>

<p>Most neural networks learn from labeled data — millions of images tagged with “cat” or “dog.” The DINO family takes a different approach called <strong>self-distillation</strong>: the model trains by trying to match a slowly-updated copy of itself, without any labels at all. The result is a model whose internal representations capture strong semantic and spatial structure, often rivaling supervised models.</p>

<p>A standard ViT (Vision Transformer) processes an image by splitting it into non-overlapping patches (typically 14×14 pixels), converting each patch into a vector, and prepending a special learnable <strong>CLS token</strong>. All tokens — CLS plus patches — interact through <strong>self-attention</strong> (each token can “look at” every other token to decide what matters) across multiple transformer layers. At the output, the CLS token serves as a global image summary (useful for classification), while patch tokens retain spatial information (useful for segmentation and finding correspondences between images). This division of labor — one global token, many spatial tokens — is important for what follows.</p>

<figure class="howtocv-figure">
  <img src="/assets/images/howtocv/approach.svg" alt="Experimental approach: ViT architecture with CLS and register tokens, showing zero-CLS and zero-registers ablation conditions" style="max-width: 700px; background: white; border-radius: 4px; padding: 0.5rem;" />
  <figcaption><strong>Figure 1.</strong> Our experimental approach. A ViT processes an image as CLS + register + patch tokens. We selectively zero CLS or register token outputs at the final layer and measure impact on global tasks (classification, retrieval) and dense tasks (correspondence, segmentation).</figcaption>
</figure>

<p>What do these patch features actually look like? The gallery below projects each model’s patch features into 3 PCA components (mapped to RGB). Notice how different models organize spatial information differently:</p>

<div id="pca-gallery" class="viz-gallery">
  <h4>Interactive: PCA patch features</h4>
  <div class="viz-controls">
    <span class="label">Image:</span>
    <button class="viz-img-btn active" data-img="img0">Fish</button>
    <button class="viz-img-btn" data-img="img1">Bird</button>
    <button class="viz-img-btn" data-img="img2">Dog</button>
    <button class="viz-img-btn" data-img="img3">Building</button>
    <button class="viz-img-btn" data-img="img4">Food</button>
  </div>
  <div class="viz-grid"></div>
</div>

<h3 id="the-artifact-problem">The artifact problem</h3>

<p><a href="https://arxiv.org/abs/2309.16588">Darcet et al. (2024)</a> discovered that DINO and DINOv2 produce <strong>high-norm artifact tokens</strong> in low-information image regions — patches corresponding to sky, water, or uniform backgrounds would develop anomalously large activation norms that distorted downstream feature maps. Their solution: append 4 learnable <strong>register tokens</strong> to the input sequence. These registers participate in the network’s computations but are discarded at inference, absorbing the artifact computation and leaving patch tokens clean. This worked — but it raised a question.</p>

<p>DINOv2 was then retrained with registers (DINOv2+reg), and the artifacts disappeared. But a question remained: <strong>what exactly are these registers doing?</strong> Are they merely absorbing garbage computation, or are they playing a more active role in the network’s information processing?</p>

<p>The patch norm heatmaps below visualize these artifacts directly. High-norm patches (bright regions) indicate where the model concentrates computation. Compare DINOv2 (artifacts visible in uniform regions) with DINOv2+reg and DINOv3 (cleaner after registers):</p>

<div id="norm-gallery" class="viz-gallery">
  <h4>Interactive: Patch norm heatmaps</h4>
  <div class="viz-controls">
    <span class="label">Image:</span>
    <button class="viz-img-btn active" data-img="img0">Fish</button>
    <button class="viz-img-btn" data-img="img1">Bird</button>
    <button class="viz-img-btn" data-img="img2">Dog</button>
    <button class="viz-img-btn" data-img="img3">Building</button>
    <button class="viz-img-btn" data-img="img4">Food</button>
  </div>
  <div class="viz-grid"></div>
</div>

<h3 id="dinov3-and-gram-anchoring">DINOv3 and Gram anchoring</h3>

<p>DINOv3 (<a href="https://arxiv.org/abs/2508.10104">Siméoni et al., 2025</a>) adds a new training objective called <strong>Gram anchoring</strong> — it encourages patch tokens to preserve their pairwise spatial relationships (which patches are similar to which). The intuition: if a model maintains consistent spatial structure during training, it should produce better features for tasks like segmentation and correspondence that depend on spatial layout.</p>

<p>DINOv3 also includes register tokens. But here’s the key question our paper investigates: <strong>how do registers interact with Gram anchoring?</strong> Does the combination simply add benefits, or does it qualitatively change how the network organizes information across token types?</p>

<hr />

<h2 id="the-experiment-token-zeroing-ablations">The Experiment: Token-Zeroing Ablations</h2>

<h3 id="the-logic-of-zeroing">The logic of zeroing</h3>

<p>Our approach is simple: at the final transformer layer, we <strong>zero out</strong> specific token types and measure the downstream impact. This lets us test what depends on each token’s contribution:</p>

<ul>
  <li><strong>Zero CLS</strong>: Sets the CLS token output to zero. If a task degrades, it relied on CLS.</li>
  <li><strong>Zero registers</strong>: Sets all 4 register outputs to zero. If a task degrades, it relied on registers.</li>
  <li><strong>Mean substitution</strong>: Replaces tokens with dataset-mean activations (rather than zeros). This preserves the statistical “scaffold” while removing image-specific content.</li>
</ul>

<p>We evaluate on four tasks spanning global and dense prediction:</p>

<table>
  <thead>
    <tr>
      <th>Task</th>
      <th>Type</th>
      <th>Metric</th>
      <th>What it measures</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>G1</strong>: Linear probe</td>
      <td>Global</td>
      <td>Top-1 accuracy</td>
      <td>Can a simple classifier read object identity from the CLS token?</td>
    </tr>
    <tr>
      <td><strong>G2</strong>: kNN retrieval</td>
      <td>Global</td>
      <td>Recall@1</td>
      <td>Can the model find the most similar image in a database?</td>
    </tr>
    <tr>
      <td><strong>D1</strong>: Correspondence</td>
      <td>Dense</td>
      <td>Accuracy</td>
      <td>Can patch features match the same object part across two images?</td>
    </tr>
    <tr>
      <td><strong>D2</strong>: Segmentation</td>
      <td>Dense</td>
      <td>mIoU (overlap score)</td>
      <td>Can patch features assign correct semantic labels to each pixel?</td>
    </tr>
  </tbody>
</table>

<h3 id="interactive-ablation-heatmap-explorer">Interactive: Ablation heatmap explorer</h3>

<p>The heatmap below shows all ablation results. Each cell encodes the accuracy (or delta from full model) for one model × ablation × task combination. Hover for exact values.</p>

<div id="heatmap-container" class="howtocv-interactive">
  <div class="howtocv-controls">
    <label class="howtocv-toggle">
      <input type="checkbox" id="heatmap-delta-toggle" />
      <span>Show as delta from full model</span>
    </label>
  </div>
  <div id="heatmap-chart"></div>
  <noscript>
    <p class="howtocv-fallback">Interactive chart requires JavaScript. Summary: zeroing CLS devastates DINOv2 across all tasks but barely affects DINOv2+reg and DINOv3 on dense tasks. Zeroing registers devastates DINOv3 across the board.</p>
  </noscript>
</div>

<h3 id="gram-anchoring-reshapes-patch-geometry">Gram anchoring reshapes patch geometry</h3>

<p>Beyond task accuracy, we wanted to understand <em>how</em> each training objective organizes information differently. <strong>Effective rank</strong> measures how many independent directions the features actually use (out of the hundreds available in the vector space) — higher means the model spreads information across more dimensions, lower means it concentrates into fewer.</p>

<figure class="howtocv-figure">
  <img src="/assets/images/howtocv/fig2_combined.svg" alt="Combined heatmap, effective rank, and eigenspectrum analysis" style="max-width: 800px;" />
  <figcaption><strong>Figure 2.</strong> (a) Task × ablation delta heatmap. (b) Effective rank of patch features — DINOv3's Gram anchoring compresses patch geometry (erank 4.0 vs 8.7 for DINOv2+reg). (c) Eigenspectrum (variance captured by each principal component) in log scale — DINOv3 concentrates information into fewer dimensions.</figcaption>
</figure>

<p>DINOv2 has an effective rank of 13.5, DINOv2+reg drops to 8.7, and DINOv3 compresses further to just 4.0. This means Gram anchoring concentrates patch information into far fewer dimensions — yet this compressed representation actually <em>improves</em> dense prediction performance. The takeaway: fewer dimensions doesn’t mean less information. DINOv3’s patches are more structured and organized, not impoverished.</p>

<hr />

<h2 id="the-asymmetric-dissociation">The Asymmetric Dissociation</h2>

<p>The heatmap shows a clear pattern — an <strong>asymmetric dissociation</strong> between CLS and register tokens. In neuroscience, a double dissociation means two systems are each necessary for different functions: damaging one impairs function A but not B, and damaging the other impairs B but not A. Our pattern is <em>asymmetric</em> rather than a clean double dissociation: CLS zeroing selectively spares dense tasks (when registers are present), but register zeroing impairs <em>both</em> global and dense tasks. This one-sided selectivity reveals that registers play a broader structural role:</p>

<h3 id="cls-zeroing-dense-tasks-are-buffered">CLS zeroing: dense tasks are buffered</h3>

<p>In DINOv2 (no registers), zeroing CLS is bad across all tasks: classification drops from 73.2% to 0.1%, and even correspondence falls from 72.0% to 56.1%. The CLS token is the network’s central hub.</p>

<p>But in DINOv2+reg and DINOv3, something different happens. CLS zeroing still kills classification (expected — the probe reads from CLS). But <strong>dense tasks are almost completely unaffected</strong>:</p>

<ul>
  <li>DINOv2+reg correspondence: 69.1% → 68.3% (−0.8pp)</li>
  <li>DINOv3 segmentation: 78.5% → 78.5% (0.0pp change)</li>
</ul>

<p>The registers have <strong>absorbed the CLS token’s role</strong> in supporting dense features. Patch tokens no longer depend on CLS for spatial computation.</p>

<h3 id="register-zeroing-everything-collapses">Register zeroing: everything collapses</h3>

<p>Conversely, zeroing registers causes large drops, especially for DINOv3:</p>

<ul>
  <li>DINOv3 classification: 62.0% → 25.4% (<strong>−36.6pp</strong>)</li>
  <li>DINOv3 segmentation: 78.5% → 47.6% (<strong>−30.9pp</strong>)</li>
  <li>DINOv3 correspondence: 78.9% → 57.8% (<strong>−21.1pp</strong>)</li>
</ul>

<p>This is the asymmetric dissociation: CLS zeroing selectively impairs global tasks while sparing dense tasks, but register zeroing impairs <em>everything</em> — global and dense alike. The asymmetry reveals that registers serve a broader structural role than CLS, functioning as computational infrastructure the entire network depends on.</p>

<p>You can see the ablation effects directly in patch PCA features below. Compare “Full Model” with “Zero CLS” (often barely changes) and “Zero Registers” (feature structure collapses):</p>

<div id="ablation-gallery" class="viz-gallery">
  <h4>Interactive: Ablation PCA features</h4>
  <div class="viz-controls">
    <span class="label">Model:</span>
    <button class="viz-model-btn" data-model="dinov2_no_reg">DINOv2</button>
    <button class="viz-model-btn" data-model="dinov2_reg">DINOv2+reg</button>
    <button class="viz-model-btn active" data-model="dinov3_vits16">DINOv3</button>
  </div>
  <div class="viz-controls">
    <span class="label">Image:</span>
    <button class="viz-img-btn active" data-img="img0">Fish</button>
    <button class="viz-img-btn" data-img="img1">Bird</button>
    <button class="viz-img-btn" data-img="img2">Dog</button>
    <button class="viz-img-btn" data-img="img3">Building</button>
    <button class="viz-img-btn" data-img="img4">Food</button>
  </div>
  <div class="viz-grid"></div>
</div>

<h3 id="the-scaffold-experiment">The scaffold experiment</h3>

<p>The zeroing results raise a key question: is it the <em>specific information</em> stored in registers that matters, or just their <em>structural presence</em> in the attention computation? We tested this with <strong>mean substitution</strong>: replacing each register’s output with its average activation pattern (computed over 5,000 images). This preserves the registers’ typical statistical footprint while stripping away any image-specific content.</p>

<p>The result: <strong>classification accuracy is fully preserved</strong> under mean substitution. DINOv2+reg drops only 0.3pp, DINOv3 actually gains 0.1pp. This means registers’ image-specific content is irrelevant — what matters is simply their <em>presence as attention targets</em>. They serve as an <strong>attention scaffold</strong>: the network routes computation through them regardless of what they contain, and removing this scaffold entirely (via zeroing) is what causes the collapse.</p>

<h3 id="measuring-the-scaffold-attention-divergence">Measuring the scaffold: attention divergence</h3>

<p>To directly quantify scaffold disruption, we measured <strong>Jensen–Shannon divergence</strong> between full and ablated attention patterns at every layer. JS divergence measures how different two probability distributions are — in this case, how much the attention pattern changes when registers are zeroed vs. mean-substituted.</p>

<p>The results are striking. Register zeroing causes <strong>cascading divergence that amplifies across layers</strong>: in DINOv3, JS divergence starts at 0.00 at layer 0 (same input, no difference yet) and grows to 0.18 by layer 11. Mean-substitution, by contrast, preserves attention patterns almost perfectly — JS stays below 0.005 at every layer. That’s a <strong>~250× gap</strong>, demonstrating that the performance collapse from zeroing is scaffold disruption, not information removal.</p>

<figure class="howtocv-figure">
  <img src="/assets/images/howtocv/fig_attention_rewiring.svg" alt="Attention scaffold under ablation: JS divergence across layers and attention redistribution" style="max-width: 700px;" />
  <figcaption><strong>Figure 3.</strong> Attention scaffold under ablation. (a) JS divergence vs. layer: register zeroing (solid) causes cascading divergence that amplifies across layers; mean-substitution (dashed) preserves attention patterns. Lighter lines show ViT-B scale. (b) CLS attention redistribution at the last layer when registers are zeroed — DINOv3's CLS attention shifts to patches (+19.9pp) while DINOv2+reg shifts toward CLS self-attention.</figcaption>
</figure>

<p>The per-register analysis aligns with the specialist findings from above: removing R2 (DINOv2+reg) or R3 (DINOv3) causes the greatest attention disruption (JS = 0.062 and 0.076 respectively), confirming these specialist registers serve as <strong>structural anchors</strong> for the attention scaffold. Remove the anchor, and the routing pattern fractures.</p>

<figure class="howtocv-figure">
  <img src="/assets/images/howtocv/correspondence/qualitative.svg" alt="Qualitative correspondence under different ablation conditions" style="max-width: 700px;" />
  <figcaption><strong>Figure 4.</strong> Qualitative correspondence results. Top: full model with correct matches (green). Middle: zero CLS — matches preserved. Bottom: zero registers — spatial matching collapses. Green = correct, red = incorrect.</figcaption>
</figure>

<hr />

<h2 id="what-do-individual-registers-do">What Do Individual Registers Do?</h2>

<p>Registers are not interchangeable. We probed each register individually and found a <strong>specialist-generalist split</strong> that reverses between architectures.</p>

<h3 id="dinov2reg-r2-is-the-specialist">DINOv2+reg: R2 is the specialist</h3>

<p>In DINOv2+reg, register R2 stands apart. Its nearest-neighbor patches are dominated by <strong>dark, low-information regions</strong> — borders, shadows, uniform backgrounds. Its cosine similarity (a measure of how aligned two vectors are, from 0 = unrelated to 1 = identical direction) to other registers is just 0.11, far below the 0.87–0.90 range among R1/R3/R4. When R2 is individually zeroed, classification drops −4.9pp; zeroing any other single register has minimal effect (&lt; 0.2pp).</p>

<p>R2 is a <strong>low-level specialist</strong>: it handles the original artifact-absorption role. R1, R3, and R4 are <strong>semantic generalists</strong> — their nearest-neighbor patches include object parts, textures, and scene elements, and they carry comparable classification information (61–64% each).</p>

<h3 id="dinov3-the-inversion">DINOv3: the inversion</h3>

<p>DINOv3 flips this pattern. R3 becomes the <strong>semantic specialist</strong> — its probe accuracy reaches 50.5%, far above R1 (4.1%) and R2 (15.2%). Conversely, R1, R2, and R4 match to <strong>low-level patches</strong>: ground textures (R4, cos=0.77), dark backgrounds (R1), and homogeneous regions (R2).</p>

<p>This is a qualitative reorganization, not just a quantitative shift. Gram anchoring changes how the network distributes computation across its register tokens.</p>

<figure class="howtocv-figure">
  <img src="/assets/images/howtocv/fig3_registers_combined.svg" alt="Per-register classification accuracy and cosine similarity matrices" style="max-width: 800px;" />
  <figcaption><strong>Figure 5.</strong> (a) Per-register classification accuracy. DINOv2+reg R2 is a low-accuracy outlier; DINOv3 R3 is the lone semantic specialist. (b–c) Pairwise cosine similarity between register representations — DINOv2+reg R2 is dissimilar to all others (cos 0.11), while DINOv3 shows a different clustering pattern.</figcaption>
</figure>

<div id="gallery-container" class="howtocv-interactive">
  <h3 class="howtocv-section-title">Interactive: Register nearest-neighbor gallery</h3>
  <p>Select a model and register to see which image patches are most similar to each register token's learned representation.</p>
  <div class="howtocv-controls">
    <div class="howtocv-btn-group" id="gallery-model-select">
      <button class="howtocv-btn active" data-model="dinov2_reg">DINOv2+reg</button>
      <button class="howtocv-btn" data-model="dinov3_vits16">DINOv3</button>
    </div>
    <div class="howtocv-btn-group" id="gallery-reg-select">
      <button class="howtocv-btn active" data-reg="r1">R1</button>
      <button class="howtocv-btn" data-reg="r2">R2</button>
      <button class="howtocv-btn" data-reg="r3">R3</button>
      <button class="howtocv-btn" data-reg="r4">R4</button>
    </div>
  </div>
  <div id="gallery-grid"></div>
  <noscript>
    <img src="/assets/images/howtocv/register_nn_static.png" alt="Register nearest-neighbor vocabulary for DINOv2+reg and DINOv3" style="max-width: 100%;" />
  </noscript>
</div>

<p><strong>How to read this gallery:</strong> Higher cosine similarity means a patch more closely matches the register’s learned representation. In DINOv2+reg, try R2 — all 5 nearest neighbors are dark, low-information patches (cos ~0.20), confirming its role as an artifact absorber. R1/R3/R4 show more diverse, semantically meaningful patches at higher similarity (~0.47). In DINOv3, R4 stands out with strong matches to green vegetation textures (cos ~0.77) — a clear texture specialist. R1/R2/R3 all weakly match earthy ground patches (cos ~0.2), suggesting they don’t specialize on any specific visual pattern. But don’t confuse visual similarity with semantic content: DINOv3’s R3 carries the most classification information (50.5% probe accuracy) despite not resembling any specific patch type. It encodes abstract semantics, not visual templates.</p>

<hr />

<h2 id="when-do-registers-become-important">When Do Registers Become Important?</h2>

<p>So registers matter — but <em>when</em> do they become important during the network’s processing? A transformer processes information through 12 sequential layers, and each layer can change what tokens attend to and what information they carry. We traced two signals across all 12 layers: <strong>attention routing</strong> (how much attention mass flows to registers) and <strong>semantic content</strong> (how much classification information each register carries). These two signals turn out to be dissociated.</p>

<h3 id="cls-attention-distribution">CLS attention distribution</h3>

<p>Before looking at per-layer dynamics, here’s the high-level picture: how does CLS distribute its attention across token types?</p>

<figure class="howtocv-figure">
  <img src="/assets/images/howtocv/fig4_attention.svg" alt="CLS attention distribution across models" style="max-width: 700px;" />
  <figcaption><strong>Figure 6.</strong> (a) CLS attention to registers vs patches. DINOv3 routes 29.1% of CLS attention to registers vs 17.9% for DINOv2+reg. (b) Per-register breakdown: DINOv2+reg concentrates on R2 (16.1%); DINOv3 concentrates on R1 (21.1%).</figcaption>
</figure>

<p>Explore the actual CLS and register attention maps across different images. Toggle between CLS attention (where the CLS token looks) and register attention (where registers collectively attend):</p>

<div id="attn-gallery" class="viz-gallery">
  <h4>Interactive: Attention map overlays</h4>
  <div class="viz-controls">
    <span class="label">Image:</span>
    <button class="viz-img-btn active" data-img="img0">Fish</button>
    <button class="viz-img-btn" data-img="img1">Bird</button>
    <button class="viz-img-btn" data-img="img2">Dog</button>
    <button class="viz-img-btn" data-img="img3">Building</button>
    <button class="viz-img-btn" data-img="img4">Food</button>
  </div>
  <div class="viz-controls">
    <span class="label">Mode:</span>
    <button class="viz-mode-btn active" data-mode="cls">CLS attention</button>
    <button class="viz-mode-btn" data-mode="regs">Register attention</button>
  </div>
  <div class="viz-grid"></div>
</div>

<h3 id="attention-flow-across-layers">Attention flow across layers</h3>

<p>To visualize this, we computed the average attention weights across 200 images, breaking down each layer’s attention into 9 flows: every token type (CLS, registers, patches) attending to every other token type.</p>

<div id="attention-container" class="howtocv-interactive">
  <h3 class="howtocv-section-title">Interactive: Attention flow across layers</h3>
  <p>Use the slider to see how attention mass redistributes across layers. Watch how patches progressively attend more to registers in DINOv3.</p>
  <div class="howtocv-controls">
    <div class="howtocv-btn-group" id="attention-model-select">
      <button class="howtocv-btn" data-model="dinov2_no_reg">DINOv2</button>
      <button class="howtocv-btn active" data-model="dinov2_reg">DINOv2+reg</button>
      <button class="howtocv-btn" data-model="dinov3_vits16">DINOv3</button>
    </div>
    <div class="howtocv-slider-row">
      <label for="attention-layer-slider">Layer:</label>
      <input type="range" id="attention-layer-slider" min="0" max="11" value="0" step="1" />
      <span id="attention-layer-label">0</span>
      <button class="howtocv-btn howtocv-btn-sm" id="attention-play-btn">&#9654; Play</button>
    </div>
  </div>
  <div id="attention-chart"></div>
  <noscript>
    <img src="/assets/images/howtocv/attention_flow_static.png" alt="Attention flow analysis across layers" style="max-width: 100%;" />
  </noscript>
</div>

<p>Key observations:</p>
<ul>
  <li><strong>DINOv2 (no registers)</strong>: CLS receives 20–36% of patch attention throughout, serving as the sole global aggregation point.</li>
  <li><strong>DINOv2+reg</strong>: Registers receive up to 17.9% of CLS attention, concentrated in R2 (16.1%). Registers are subordinate to CLS.</li>
  <li><strong>DINOv3</strong>: Register attention builds <strong>gradually</strong> from mid-layers, reaching 28.7% of total patch attention by layer 11. R1 alone captures 21.1%. Registers become a major computational pathway.</li>
</ul>

<h3 id="layer-wise-probing-when-does-semantic-content-emerge">Layer-wise probing: when does semantic content emerge?</h3>

<p>To measure when registers actually acquire meaningful content, we trained simple classifiers (linear probes) on each register’s output at 7 intermediate layers. If a register carries class information at a given layer, the probe should achieve above-chance accuracy.</p>

<div id="probe-container" class="howtocv-interactive">
  <h3 class="howtocv-section-title">Interactive: Layer-wise register probing</h3>
  <p>Drag the slider to see classification accuracy at each layer. Note the sharp jump at layers 10–11 and the transient R1 peak in DINOv3.</p>
  <div id="probe-chart"></div>
  <noscript>
    <img src="/assets/images/howtocv/layer_probe_static.png" alt="Layer-wise probing results for register tokens" style="max-width: 100%;" />
  </noscript>
</div>

<h3 id="layer-wise-task-performance">Layer-wise task performance</h3>

<p>The layer sweep adds another view: how does <em>task</em> performance (not just register probing) evolve across layers?</p>

<figure class="howtocv-figure">
  <img src="/assets/images/howtocv/fig5_layer_sweep.svg" alt="Layer-wise classification and correspondence performance" style="max-width: 700px;" />
  <figcaption><strong>Figure 7.</strong> (a) CLS classification accuracy across layers — all models are near-random until layer 8, then rise steeply. (b) Patch correspondence peaks at mid-layers (6–8) then declines for DINOv2 variants, while DINOv3 maintains the best late-layer correspondence thanks to Gram anchoring.</figcaption>
</figure>

<h3 id="the-temporal-dissociation">The temporal dissociation</h3>

<p>The attention and probing data show a disconnect between <em>where the network looks</em> and <em>what it knows</em>:</p>

<ol>
  <li><strong>Attention routing builds gradually</strong>: Patches start attending to registers from mid-layers onward, ramping smoothly.</li>
  <li><strong>Semantic content emerges abruptly</strong>: All tokens carry near-random classification accuracy through layer 8 (&lt; 6% for DINOv2+reg, &lt; 14% for DINOv3). Then at layers 10–11, accuracy jumps sharply — CLS jumps to 67.3%/61.9%, and specific registers follow.</li>
</ol>

<p>These two signals are <strong>temporally dissociated</strong>: the attention routing infrastructure is built several layers before any semantic content appears. Registers are being used as computation targets before they carry meaningful information.</p>

<p>The per-register dynamics are telling:</p>

<ul>
  <li><strong>DINOv3 R1</strong>: Peaks at layer 10 (10.7% accuracy) then <strong>drops</strong> to 4.1% at layer 11 — despite receiving the most attention (21.1%). R1 appears to be a <strong>transient computation buffer</strong> — it temporarily holds and processes information before passing it along, rather than accumulating a final answer.</li>
  <li><strong>DINOv3 R3</strong>: Progressively accumulates from 28.9% (layer 10) to 50.5% (layer 11) — a <strong>semantic accumulator</strong>.</li>
  <li><strong>DINOv2+reg R1/R3/R4</strong>: All acquire and retain classification information (61–64% at layer 11) — <strong>semantic endpoints</strong>.</li>
</ul>

<p>This suggests registers serve dynamic computational roles that change across layers, not fixed storage functions.</p>

<hr />

<h2 id="cumulative-vs-individual-register-effects">Cumulative vs. Individual Register Effects</h2>

<p>When we zero registers one at a time, the effects are modest — but they reveal which registers matter most for which tasks.</p>

<figure class="howtocv-figure">
  <img src="/assets/images/howtocv/fig6_register_lesion.svg" alt="Per-register lesion effects on classification and correspondence" style="max-width: 700px;" />
  <figcaption><strong>Figure 8.</strong> Per-register lesion effects. (a) CLS classification: DINOv2+reg R2 is the only individual register whose removal substantially hurts (−4.9pp). DINOv3 effects are distributed. (b) Correspondence: similar pattern — no single DINOv3 register is critical alone.</figcaption>
</figure>

<p>But zeroing all four together produces a collapse far exceeding the sum of individual effects:</p>

<ul>
  <li><strong>DINOv2+reg</strong>: Sum of individual G1 deltas = −5.2pp, collective = −18.9pp</li>
  <li><strong>DINOv3</strong>: Sum of individual = −7.0pp, collective = −36.6pp</li>
</ul>

<p>This <strong>non-additive interaction</strong> — the collective effect far exceeds the sum of individual effects — confirms that registers function as a coordinated system. No single register is critical on its own — what matters is the collective scaffold.</p>

<figure class="howtocv-figure">
  <img src="/assets/images/howtocv/cumulative_lesion.svg" alt="Cumulative register lesion effects" style="max-width: 600px;" />
  <figcaption><strong>Figure 9.</strong> Cumulative register lesion: zeroing registers one at a time. Individual effects are small, but collective zeroing (rightmost) far exceeds their sum, revealing non-additive interactions.</figcaption>
</figure>

<hr />

<h2 id="does-this-scale-controls-and-validation">Does This Scale? Controls and Validation</h2>

<h3 id="vit-s-vs-vit-b">ViT-S vs ViT-B</h3>

<p>We replicated all key experiments with ViT-B backbones. The ablation patterns are consistent across scales.</p>

<figure class="howtocv-figure">
  <img src="/assets/images/howtocv/fig7_scale_comparison.svg" alt="ViT-S vs ViT-B scale comparison" style="max-width: 700px;" />
  <figcaption><strong>Figure 10.</strong> Scale comparison. Solid = ViT-S, dashed = ViT-B. (a) Classification: register zeroing drops are larger for DINOv3 at both scales. (b) Segmentation: DINOv3 shows the largest register-zeroing drop at both scales. The asymmetric dissociation pattern is scale-invariant.</figcaption>
</figure>

<h3 id="is-register-zeroing-special">Is register zeroing special?</h3>

<p>A natural concern: maybe zeroing <em>any</em> set of 4 tokens would be equally disruptive? We controlled for this by zeroing 4 randomly selected patch tokens instead of registers.</p>

<figure class="howtocv-figure">
  <img src="/assets/images/howtocv/fig9_random_patch_control.svg" alt="Random patch zeroing negative control" style="max-width: 600px;" />
  <figcaption><strong>Figure 11.</strong> Negative control: zeroing 4 random patch tokens has negligible effect compared to zeroing 4 register tokens. The register effects are specific to the registers, not a general consequence of removing tokens from the sequence.</figcaption>
</figure>

<hr />

<h2 id="practical-takeaways">Practical Takeaways</h2>

<p>Some practical implications:</p>

<ol>
  <li>
    <p><strong>Don’t discard registers.</strong> If your pipeline extracts features from register-equipped models, include register tokens in your feature set. They are not auxiliary — they are load-bearing.</p>
  </li>
  <li>
    <p><strong>Mean substitution works as a fallback.</strong> If you must replace register values (e.g., for batching across models with different register counts), substituting dataset-mean activations preserves the attention scaffold and maintains accuracy.</p>
  </li>
  <li>
    <p><strong>Register specialization is exploitable.</strong> DINOv2+reg’s R2 encodes low-level image statistics; DINOv3’s R3 concentrates semantic content. These can be selectively queried for different downstream tasks.</p>
  </li>
  <li>
    <p><strong>Registers are active, not passive.</strong> The temporal dissociation — attention routing precedes semantic emergence — means registers participate in computation, not just storage. This has implications for pruning, distillation, and architectural design.</p>
  </li>
  <li>
    <p><strong>Scale-consistent.</strong> All findings replicate across ViT-S and ViT-B backbones, suggesting these are stable properties of the architecture rather than scale-dependent phenomena.</p>
  </li>
</ol>

<hr />

<h2 id="citation">Citation</h2>

<div class="language-bibtex highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">@inproceedings</span><span class="p">{</span><span class="nl">parodi2026cls</span><span class="p">,</span>
  <span class="na">title</span><span class="p">=</span><span class="s">{CLS and Register Token Ablations Reveal Asymmetric
         Information Flow in Vision Transformers}</span><span class="p">,</span>
  <span class="na">author</span><span class="p">=</span><span class="s">{Parodi, Felipe and Segado, Melanie}</span><span class="p">,</span>
  <span class="na">booktitle</span><span class="p">=</span><span class="s">{CVPR HOW Workshop}</span><span class="p">,</span>
  <span class="na">year</span><span class="p">=</span><span class="s">{2026}</span>
<span class="p">}</span>
</code></pre></div></div>

<div class="howtocv-footer">
  <p>Built with PyTorch, HuggingFace Transformers, and DINOv3. Interactive visualizations powered by Plotly.js and D3.js.</p>
</div>

<!-- Load interactive scripts -->
<script src="https://cdn.plot.ly/plotly-2.27.0.min.js"></script>

<script src="https://d3js.org/d3.v7.min.js"></script>

<script src="/assets/js/howtocv-heatmap.js"></script>

<script src="/assets/js/howtocv-probes.js"></script>

<script src="/assets/js/howtocv-attention.js"></script>

<script src="/assets/js/howtocv-gallery.js"></script>

<script src="/assets/js/howtocv-viz-gallery.js"></script>]]></content><author><name>Felipe Parodi</name></author><category term="blog" /><category term="vision-transformers" /><category term="interpretability" /><category term="DINOv3" /><category term="self-supervised-learning" /><summary type="html"><![CDATA[DINOv3's register tokens function as active computation buffers, not passive stores — and the evidence comes from a simple trick: zeroing them out.]]></summary></entry></feed>